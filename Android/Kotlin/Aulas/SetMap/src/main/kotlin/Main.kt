fun main(args: Array<String>) {
    /*var itens = hashSetOf("a", "b,", "c", "c") //não repete os itens

    for (item in itens){
        println(item)
    }
    println(itens)*/

    //map
    var map = hashMapOf("urso" to "Animal", "cao" to "bestfriend")
    println(map)
}